[project]
name = "osa-psg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mahyar Mirzazadeh", email = "mahyarrk@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
]

[project.scripts]
osa-psg = "osa_psg:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
